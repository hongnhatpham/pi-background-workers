{
  "name": "pi-background-workers",
  "private": true,
  "type": "module",
  "main": "./index.ts",
  "exports": "./index.ts",
  "description": "Background worker delegation for Pi",
  "keywords": ["pi-package"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "check": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "@types/node": "^24.5.2",
    "tsx": "^4.20.6",
    "typescript": "^5.9.2"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  }
}
